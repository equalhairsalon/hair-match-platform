# R0.5 Pilot Recruitment Ready

## 1. Neon migration
在 Neon -> Query 執行：

`database/R0.5_MIGRATION.sql`

R0.3 / R0.4 migration 尚未執行的話，請依序先執行 R0.3、R0.4，再執行 R0.5。

## 2. 平台管理員
先用網站註冊一個你自己的帳號，然後在 Neon Query 執行（把 EMAIL 換成你的登入 Email）：

```sql
update users set role='admin' where lower(email)=lower('YOUR_EMAIL');
```

登出再重新登入後即可開：`/admin`

## 3. Vercel Blob
若尚未建立：Vercel -> hair-match-platform -> Storage -> Create Database/Storage -> Blob。
連接成功後確認 `BLOB_READ_WRITE_TOKEN` 已自動出現在 Environment Variables。

## 4. 第一批設計師流程
設計師註冊 -> `/pro/onboarding` -> 上傳大頭照/封面、地址、GPS -> `/pro/profile` 上作品/價目 -> Admin `/admin` 審核 -> 通過後才進 `/discover`。

## 5. 上線
```bash
git add -A
git commit -m "R0.5 pilot recruitment ready"
git push origin main
```
