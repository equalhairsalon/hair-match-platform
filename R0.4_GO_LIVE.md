# R0.4 上線步驟

1. Neon SQL Editor 執行 `database/R0.4_MIGRATION.sql`。
2. Vercel 專案確認現有 `DATABASE_URL` 與 `AUTH_SECRET`。
3. 若要正式上傳作品與顧客照片：Vercel → Storage → Create Database/Storage → 選 Vercel Blob → Connect to `hair-match-platform`，確認產生 `BLOB_READ_WRITE_TOKEN`。
4. 本機測試：`npm install` 後 `npm run dev`；同 Wi-Fi 版可用 `npm run dev:lan`，但手機 GPS 建議用正式 HTTPS Vercel 網址測。
5. `git add . && git commit -m "R0.4 geo portfolio chat cross-platform" && git push`。
6. Vercel 自動部署後，以 iPhone Safari 與 Android Chrome 各測一次：註冊、定位、拍照/相簿、發布、報價、聊天、選擇方案。

## 注意
- LAN 的 `http://192.168.x.x:3000` 不是 secure context，部分手機會封鎖 GPS / 相機 API；正式 `https://hair-match-platform.vercel.app` 才是完整權限測試入口。
- PWA 可加入主畫面；原生 iOS / Android 之後共用 Neon 與既有 API，不需要重做後端資料。
