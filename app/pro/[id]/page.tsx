import { Header, MobileNav } from '@/components/Header'; import { providers } from '@/lib/demo-data';
export default async function ProviderPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const p=providers.find(x=>x.id===id)||providers[0];return <div className="app-shell"><Header/><main className="container page"><div className="profile-hero"><img src={p.cover}/><div className="profile-overlay"><div className="provider-row"><img className="avatar" src={p.avatar}/><div><h1 style={{margin:0}}>{p.name}｜{p.salon}</h1><div>★ {p.rating}（{p.reviews}） · 完成 {p.completed} 次 · {p.distanceKm} km</div></div></div></div></div><div className="profile-main"><div><div className="panel"><h3>關於我</h3><p className="muted" style={{lineHeight:1.8}}>{p.intro}</p><div className="tags">{p.specialties.map(x=><span className="tag" key={x}>{x}</span>)}</div></div><div className="panel" style={{marginTop:16}}><h3>作品集</h3><div className="work-grid">{p.works.map(x=><img src={x} key={x}/>)}</div></div></div><aside><div className="panel summary-box"><div className="status-pill" style={{position:'static',display:'inline-block'}}>{p.isAvailableNow?'現在可接':'今天可預約'}</div><h2 style={{marginBottom:4}}>{p.availableText}</h2><p className="small">{p.location}</p><button className="btn btn-primary" style={{width:'100%',margin:'10px 0'}}>邀請他報價</button><button className="btn" style={{width:'100%'}}>先聊聊</button><h3 style={{marginTop:22}}>服務價格</h3>{p.services.map(s=><div className="summary-row" key={s.label}><span>{s.label}</span><strong>{s.price}</strong></div>)}<div className="summary-row"><span>Instagram</span><strong>{p.instagram}</strong></div></div></aside></div></main><MobileNav/></div>}
